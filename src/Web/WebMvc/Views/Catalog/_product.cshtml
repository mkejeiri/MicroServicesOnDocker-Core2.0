@model MicroServicesOnDocker.Web.WebMvc.Models.CatalogItem


<img src="@Model.PictureUri" alt="missing" class="esh-catalog-thumbnail" />
<div class="esh-catalog-name">
    <span>@Model.Name</span>
</div>
<div>
    <span>@Model.Price.ToString("N2")</span>
</div>

